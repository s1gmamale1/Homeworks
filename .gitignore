# Virtualenv
.venv/
__pycache__/
*.pyc
*.pyo

# Database (large + contains user data)
nets.db
nets.db-wal
nets.db-shm
*.sqlite
*.sqlite3

# Secrets
.env
.env.*
!.env.example
.passwd
claw_api_service.json
*service_account*.json
*.pem
*.key

# Logs
*.log
uvicorn.log
nohup.out

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
*.swp

# Windows installers / binaries (don't belong in source control)
*.msi
*.exe

# Obsidian vault (local layout, not for the repo)
.obsidian/
notes/

# Claude Code local state (worktrees, agents, plans)
.claude/

# Codex local memory / rule bundle
.codex/

# Build / cache
.pytest_cache/
.mypy_cache/
.ruff_cache/
node_modules/
CLAUDE.md
CLAUDE.local.md
memory/
tmp/

# Generated / runtime
data/
# But DO commit the bundled gate-quote library shipped under server/data/
!server/data/
!server/data/**

# Test artifacts
tests/last_run_report.json
# Local replay fixtures pulled from prod — never commit prod content
tests/fixtures/prod_replay/

# Scratch / reference snippets
ref_*.txt
ai_models_comparison.md
SENTENCE_FILL_BACKEND_PLAN.md

# Local-only working artifacts (not project-relevant)
/extract_lesson.py
/pip_err.txt
/pip_out.txt

# Architectural rework planning docs (kept local; not part of the codebase)
/homeworks_ai_architecture_plan_pr169_adjusted/
.mcp.json

# DB backups (one-shot migration snapshots like mojibake repair 2026-05-13)
nets.db.bak.*

# Uvicorn run logs from local dev (caught by *.log too, but explicit for clarity)
uvicorn.stdout.log
uvicorn.stderr.log